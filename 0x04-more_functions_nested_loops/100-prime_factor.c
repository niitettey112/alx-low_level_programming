#include <stdio.h>
#include <math.h>

/**
 * main - print the largest number
 *
 * Return: 0
 */
int main(void)
{
	long i, maxf;
	long number = 612852475143;
	double square = sqrt(number);

	for (i = 1; i <= square; i++)
	{
		if (number % x == 0)
		{
			maxf = number / x;
		}
	}
	printf("%ld\n", maxf);

	return (0);
}
